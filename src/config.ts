import { VPS_URL, API_KEY, BASE_URL, CLIENT_ID } from '@env';

export const DEFAULT_VPS_URL = VPS_URL;
export const DEFAULT_API_KEY = API_KEY;
export const DEFAULT_BASE_URL = BASE_URL;
export const DEFAULT_CLIENT_ID = CLIENT_ID;

